// This is a temporary mock file until Convex generates the real server
// It will be replaced when you run `npx convex dev` and create a project

export const mutation = (config: any) => config;
export const query = (config: any) => config; 